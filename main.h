#ifndef HEADER
#define HEADER

#include <stdio.h>
#include <stdarg.h>
#include <stdlib.h>

int _putchar(char a);

int _printf(const char *format, ...);

int print_string(char *string);

int print_int(int number);
#endif
